mirrors:
  "*":
    endpoint:
      - "http://registry.localhost:5000"

  "docker.io":
    endpoint:
      - "http://proxy-registry.localhost:5001"

configs:
  "registry1.dsop.io":
    auth:
      username: ${REGISTRY1_USER}
      password: ${REGISTRY1_PASSWORD}