configs:
  "registry1.dsop.io":
    auth:
      username: robot${DOLLAR}bigbang
      password: ${REGISTRY1_PASSWORD}
  "registry1.dso.mil":
    auth:
      username: robot${DOLLAR}bigbang
      password: ${REGISTRY1_PASSWORD}