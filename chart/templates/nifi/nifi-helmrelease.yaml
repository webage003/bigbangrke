{{- if .Values.nifi.enabled }}
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nifi
  namespace: {{ .Release.Namespace }}
spec:
  targetNamespace: nifi
  chart:
    spec:
      chart: {{ .Values.nifi.git.path }}
      interval: 5m
      sourceRef:
        kind: GitRepository
        name: nifi
        namespace: {{ .Release.Namespace }}

  {{- with .Values.flux }}
  interval: {{ .interval }}
  test:
    enable: false
  install:
    remediation:
      retries: {{ .install.retries }}
  upgrade:
    remediation:
      retries: {{ .upgrade.retries }}
      remediateLastFailure: true
    cleanupOnFail: true
  rollback:
    timeout: {{ .rollback.timeout }}
    cleanupOnFail: {{ .rollback.cleanupOnFail }}
  {{- end }}

  valuesFrom:
    - name: values
      kind: Secret
      valuesKey: "nifi.yaml"

  values:
    createNamespace: false
{{- end }}
