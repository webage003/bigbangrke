{{- if .Values.nifi.enabled }}
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: nifi
  namespace: {{ .Release.Namespace }}
spec:
  interval: {{ .Values.flux.interval }}
  url: {{ .Values.nifi.git.repo }}
  ref:
  {{- include "validRef" .Values.nifi.git | nindent 4 }}
  {{ include "gitIgnore" . }}
  {{- include "gitCreds" .Values.git | nindent 2 }}
  secretRef:
    name: appsrepo
{{- end }}
