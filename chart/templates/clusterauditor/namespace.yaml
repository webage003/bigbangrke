{{- if .Values.addons.clusterAuditor.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/name: cluster-auditor
    app.kubernetes.io/component: "core"
    {{- include "commonLabels" . | nindent 4}}
    istio-injection: disabled
  name: cluster-auditor
{{- end }}
