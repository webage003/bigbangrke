
  {{- if and .Values.istio.enabled ( or .Values.addons.authservice.enabled ( or .Values.monitoring.sso.enabled .Values.istio.sso.enabled ) ) }}
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: authservice
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: authservice
    app.kubernetes.io/component: "core"
    {{- include "commonLabels" . | nindent 4}}
spec:
  targetNamespace: authservice
  chart:
    spec:
      chart: {{ .Values.addons.authservice.git.path }}
      interval: 5m
      sourceRef:
        kind: GitRepository
        name: authservice
        namespace: {{ .Release.Namespace }}

  {{- with .Values.flux }}
  interval: {{ .interval }}
  test:
    enable: false
  install:
    remediation:
      retries: {{ .install.retries }}
  upgrade:
    remediation:
      retries: {{ .upgrade.retries }}
      remediateLastFailure: true
    cleanupOnFail: true
  rollback:
    timeout: {{ .rollback.timeout }}
    cleanupOnFail: {{ .rollback.cleanupOnFail }}
  {{- end }}

  valuesFrom:
    - name: {{ .Release.Name }}-authservice-values
      kind: Secret
      valuesKey: "common"
    - name: {{ .Release.Name }}-authservice-values
      kind: Secret
      valuesKey: "defaults"
    - name: {{ .Release.Name }}-authservice-values
      kind: Secret
      valuesKey: "overlays"
  
  values:
    {{ if and .Values.addons.authservice.redis.enabled  .Values.addons.authservice.redis.redis_server_uri}}
     redis:
       enabled: true
     redis_server_uri:  {{ .Values.addons.authservice.redis.redis_server_uri}}
   {{ else if .Values.addons.authservice.redis.redis_server_uri }}
     redis:
     redis_server_uri: {{ .Values.addons.authservice.redis.redis_server_uri}}
  {{ end }}
  {{- if .Values.istio.enabled }}
  dependsOn:
  - name: istio
    namespace: {{ .Release.Namespace }}
  {{- end }}
{{- end }}